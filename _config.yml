name: Rich
markdown: kramdown
github_username: richryan

gems:
  - jekyll-feed
  # - github-pages
exclude:
  - Gemfile
  - Gemfile.lock

collections:
  events:
    output: true
    permalink: /events/:path/
    title: Events
